{
  "name": "obsidian-rag-plugin",
  "version": "0.2.0",
  "description": "Sidebar extensible para Obsidian que consulta el RAG local — notas relacionadas (find_related), búsqueda semántica, multi-backend (HTTP/CLI/MCP).",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "keywords": ["obsidian", "rag", "mcp", "sidebar", "related-notes"],
  "author": "Fernando Ferrari",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.20.0",
    "obsidian": "latest",
    "tslib": "^2.6.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
